export enum CalendarNavigationType {
    Left = "left",
    Both = "both",
    Right = "right",
}

export enum NavigationDirection {
    Prev = "prev",
    Next = "next",
}

export enum SupportedLanguages {
    En = "en",
    Cs = "cs",
}